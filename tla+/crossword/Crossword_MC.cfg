SPECIFICATION Spec

CONSTANTS 
    Replicas = {r1, r2, r3}
    Values = {v1, v2}
    Slots = {s1}
    Ballots <- ConstBallots
    Shards = {c1, c2, c3}
    NumDataShards <- ConstNumDataShards
    MaxFaults <- ConstMaxFaults

SYMMETRY SymmetricPerms

INVARIANTS 
    TypeOK
    NontrivialityInv
    ConsistencyInv

\* PROPERTIES 
\*     ConsensusSpec  \* check this only on very small inputs

CHECK_DEADLOCK FALSE
