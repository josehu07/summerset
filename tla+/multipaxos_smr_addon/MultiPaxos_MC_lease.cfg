SPECIFICATION Spec

CONSTANTS 
    Replicas = {s1, s2, s3}
    Writes = {w1, w2}
    Reads = {r1}
    MaxBallot <- ConstMaxBallot
    ReadQuorumSize <- ConstReadQuorumSizeUsual
    CommitNoticeOn <- TRUE
    NodeFailuresOn <- TRUE
    StableLeaderOn <- TRUE

SYMMETRY SymmetricPerms

INVARIANTS 
    TypeOK
    Linearizability

CHECK_DEADLOCK TRUE
